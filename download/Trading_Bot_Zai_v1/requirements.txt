# Trading Bot Zai v1 — Requirements
# Install: pip install -r requirements.txt

# Core
numpy>=1.24.0
flask>=3.0.0

# Quotex API
pyquotex>=0.1.0

# HTTP client (used by pyquotex)
httpx>=0.25.0

# .env file support
python-dotenv>=1.0.0

# SSL/TLS
certifi>=2023.7.22

# Browser-based session extraction (optional, for CloudFlare bypass)
playwright>=1.40.0

# Browser fingerprint evasion (optional, enhances Playwright)
playwright-stealth>=1.0.6

# curl_cffi — alternative HTTP with TLS fingerprinting (optional)
# Uncomment if you need browser-like TLS fingerprints:
# curl_cffi>=0.5.0
